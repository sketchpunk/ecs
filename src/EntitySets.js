
// Able to setup Collections of Entities that share the same set of Components.
// This might be more of a manual thing, then automatic.

class Set{
    name        = "";
    bitmask     = null;
    entities    = []; 
}

class EntitySets{
    items = {};
}

return EntitySets;